# Run from repo root: pip install -r interfaces/discord/requirements.txt
discord.py>=2.3.2,<3
httpx>=0.27.0,<1
python-dotenv>=1.0.0,<2
# Optional: DATABASE_URL link-check needs the same driver as AgentLayer
# psycopg[binary]>=3.1.0,<4
